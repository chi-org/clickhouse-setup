<clickhouse>
    <zookeeper>
        {% for host in groups['all'] %}
             <node index="{{ hostvars[host]['clickhouse']['server_id'] }}">
                <host>{{ host }}</host>
                <port>9181</port>
            </node>
        {% endfor %}
    </zookeeper>
</clickhouse>